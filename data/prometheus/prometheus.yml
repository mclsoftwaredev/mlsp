global:
  scrape_interval: 5s
  evaluation_interval: 10s

scrape_configs:
  - job_name: 'Mlsp Service'
    metrics_path: '/mlsp/v1/actuator/prometheus'
    static_configs:
      - targets: [ 'docker.for.mac.localhost:8080' ]